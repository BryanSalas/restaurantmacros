define(['app'], function (app) {
    app.controller('rmHome', function($scope) {
    $scope.header = "Currently remaking site, will be up soon!"
        //$scope.header = "Counting macros, made easy.";
    });
});